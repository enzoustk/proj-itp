#include <iostream>
using namespace std;

int main(){
    int n = 10;
    char matriz[n][n];
    for (int i = 0;i < n;i++){
        for (int j = 0;j < n;j++){
            matriz[i][j] = '~';
        }
    }
    for(int k = 0;k <= 0;k++){
        int i,j;
        cin >> i >> j;
        matriz[i][j] = '*';
    }
    for(int k = 0;k <= 0;k++){
        int i,j;
        cin >> i >> j;
        matriz[i][j];
    }
    for(int k = 0;k <= 0;k++){
        int i,j;
        cin >> i >> j;
        matriz[i][j];
    }
    for(int k = 0;k <= 0;k++){
        int i,j,;
        int x,z;
        int a,d;
        int l,c;
        cin >> i >> j;
        matriz[i][j];
    }
    for (int i = 0;i < n;i++){
        for (int j = 0;j < n;j++){
            cout << matriz[i][j];
        }
    }
    return 0;
}